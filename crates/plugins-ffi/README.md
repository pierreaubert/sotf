# plugins-ffi

C FFI bindings for SOTF audio plugins (for Audio Unit integration).

macOS-only FFI layer exposing SOTF plugins through opaque handles for use in Audio Unit hosts. Supports JSON-based configuration and parameter management across the C boundary.
