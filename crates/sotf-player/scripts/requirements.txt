# Python dependencies for sotf-player/scripts/cli.py, flux2_server.py, mflux_server.py and gemma_server.py

# Audio metadata reading (optional; cli.py degrades gracefully without it)
mutagen

# Local FLUX image-generation backend (flux2_server.py)
torch
diffusers

# Local MLX image-generation backend (mflux_server.py)
mflux

# Local text-generation backend (gemma_server.py)
transformers
accelerate
mlx-lm; sys_platform == 'darwin'

# Shared image I/O
Pillow
