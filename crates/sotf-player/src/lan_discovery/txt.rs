// Auto-generated by r2factor. Manual edits will be overwritten on next split.


#[cfg(any(not(target_os = "macos"), test))]
pub(super) fn txt_record_bytes(records: &[String]) -> Vec<u8> {
    let mut bytes = Vec::new();
    for record in records {
        let record = record.as_bytes();
        let len = record.len().min(255);
        bytes.push(len as u8);
        bytes.extend_from_slice(&record[..len]);
    }
    bytes
}

pub(super) fn txt_value(value: &str, fallback: &str) -> String {
    let value = value.trim();
    if value.is_empty() {
        fallback.to_string()
    } else {
        value.chars().filter(|c| *c != '\0').collect()
    }
}

