// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use tokio::io::{AsyncReadExt, AsyncWriteExt};
use tokio::net::TcpStream;

use super::auth::auth_get;

pub(super) async fn read_http_response(addr: std::net::SocketAddr, request: String) -> String {
    let mut stream = TcpStream::connect(addr).await.unwrap();
    stream.write_all(request.as_bytes()).await.unwrap();
    let mut response = Vec::new();
    tokio::time::timeout(
        std::time::Duration::from_secs(2),
        stream.read_to_end(&mut response),
    )
    .await
    .unwrap()
    .unwrap();
    String::from_utf8(response).unwrap()
}

pub(super) async fn read_until(stream: &mut TcpStream, needle: &str) -> String {
    let mut response = Vec::new();
    tokio::time::timeout(std::time::Duration::from_secs(2), async {
        let mut buf = [0_u8; 1024];
        loop {
            let n = stream.read(&mut buf).await.unwrap();
            if n == 0 {
                break;
            }
            response.extend_from_slice(&buf[..n]);
            if String::from_utf8_lossy(&response).contains(needle) {
                break;
            }
        }
    })
    .await
    .unwrap();
    String::from_utf8(response).unwrap()
}

pub(super) async fn connect_sse_client(addr: std::net::SocketAddr) -> TcpStream {
    let mut stream = TcpStream::connect(addr).await.unwrap();
    stream
        .write_all(auth_get("/api/v1/events").as_bytes())
        .await
        .unwrap();
    let response = read_until(&mut stream, "event: state").await;
    assert!(response.starts_with("HTTP/1.1 200 OK"));
    assert!(response.contains("Content-Type: text/event-stream"));
    stream
}
