// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use sha2 :: { Digest , Sha256 } ;

pub(super) const REMOTE_SERVER_STORE_VERSION: u32 = 1;

pub(super) const REMOTE_TOKEN_STORE_VERSION: u32 = 1;

pub(super) fn remote_server_id(api_base_url: &str) -> String {
    let digest = Sha256::digest(api_base_url.as_bytes());
    let mut out = String::from("sotf:");
    for byte in digest.iter().take(8) {
        out.push_str(&format!("{byte:02x}"));
    }
    out
}

pub(super) fn redacted_token(token: &str) -> String {
    if token.is_empty() {
        "<empty>".to_string()
    } else {
        format!("<redacted:{}>", token.len())
    }
}
