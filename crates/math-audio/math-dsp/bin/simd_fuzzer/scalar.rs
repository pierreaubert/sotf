// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use rustfft::num_complex::Complex;

pub(super) fn scalar_complex_mul_add(
    dst: &mut [Complex<f32>],
    src: &[Complex<f32>],
    hrtf: &[Complex<f32>],
) {
    for i in 0..dst.len() {
        dst[i] += src[i] * hrtf[i];
    }
}

pub(super) fn scalar_complex_mul(
    dst: &mut [Complex<f32>],
    src: &[Complex<f32>],
    hrtf: &[Complex<f32>],
) {
    for i in 0..dst.len() {
        dst[i] = src[i] * hrtf[i];
    }
}

pub(super) fn scalar_complex_mul_inplace(dst: &mut [Complex<f32>], hrtf: &[Complex<f32>]) {
    for i in 0..dst.len() {
        dst[i] *= hrtf[i];
    }
}

pub(super) fn scalar_scale_add(dst: &mut [f32], src: &[f32], scale: f32) {
    for i in 0..dst.len() {
        dst[i] += src[i] * scale;
    }
}

pub(super) fn scalar_blend(dst: &mut [f32], prev: &[f32], alpha: f32) {
    for i in 0..dst.len() {
        dst[i] = prev[i] + alpha * (dst[i] - prev[i]);
    }
}

pub(super) fn scalar_window_mul(dst: &mut [f32], src: &[f32], window: &[f32]) {
    for i in 0..dst.len() {
        dst[i] = src[i] * window[i];
    }
}

pub(super) fn scalar_window_mul_inplace(data: &mut [f32], window: &[f32]) {
    let len = data.len().min(window.len());
    for i in 0..len {
        data[i] *= window[i];
    }
}

pub(super) fn scalar_deinterleave_stereo(input: &[f32], left: &mut [f32], right: &mut [f32]) {
    for (i, chunk) in input.chunks_exact(2).enumerate() {
        left[i] = chunk[0];
        right[i] = chunk[1];
    }
}

pub(super) fn scalar_apply_gain(buffer: &mut [f32], gain: f32) {
    for val in buffer.iter_mut() {
        *val *= gain;
    }
}

pub(super) fn scalar_apply_per_channel_gain(buffer: &mut [f32], channels: usize, gains: &[f32]) {
    let num_frames = buffer.len() / channels;
    for frame in 0..num_frames {
        for ch in 0..channels {
            buffer[frame * channels + ch] *= gains[ch];
        }
    }
}

pub(super) fn scalar_compute_covariance(
    left: &[Complex<f32>],
    right: &[Complex<f32>],
    start: usize,
    end: usize,
) -> (f32, f32, Complex<f32>) {
    let mut cov_xx = 0.0_f32;
    let mut cov_yy = 0.0_f32;
    let mut cov_xy = Complex::new(0.0, 0.0);
    for i in start..end {
        cov_xx += left[i].norm_sqr();
        cov_yy += right[i].norm_sqr();
        cov_xy += left[i] * right[i].conj();
    }
    (cov_xx, cov_yy, cov_xy)
}

pub(super) fn scalar_flush_denormals(samples: &mut [f32]) {
    const DENORM_THRESHOLD: f32 = 1e-30;
    for sample in samples.iter_mut() {
        if sample.abs() < DENORM_THRESHOLD {
            *sample = 0.0;
        }
    }
}
