// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use rustfft::num_complex::Complex;

pub(super) fn max_abs_diff_complex(a: &[Complex<f32>], b: &[Complex<f32>]) -> f32 {
    a.iter()
        .zip(b.iter())
        .map(|(x, y)| (x.re - y.re).abs().max((x.im - y.im).abs()))
        .fold(0.0_f32, f32::max)
}

pub(super) fn max_rel_diff_complex(a: &[Complex<f32>], b: &[Complex<f32>]) -> f32 {
    a.iter()
        .zip(b.iter())
        .map(|(x, y)| {
            let denom_re = x.re.abs().max(y.re.abs()).max(1e-30);
            let denom_im = x.im.abs().max(y.im.abs()).max(1e-30);
            ((x.re - y.re).abs() / denom_re).max((x.im - y.im).abs() / denom_im)
        })
        .fold(0.0_f32, f32::max)
}

pub(super) fn max_abs_diff_f32(a: &[f32], b: &[f32]) -> f32 {
    a.iter()
        .zip(b.iter())
        .map(|(x, y)| (x - y).abs())
        .fold(0.0_f32, f32::max)
}

pub(super) fn max_rel_diff_f32(a: &[f32], b: &[f32]) -> f32 {
    a.iter()
        .zip(b.iter())
        .map(|(x, y)| {
            let denom = x.abs().max(y.abs()).max(1e-30);
            (x - y).abs() / denom
        })
        .fold(0.0_f32, f32::max)
}
