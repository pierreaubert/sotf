// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use super::misc::sum_objectives;
use super::types::Individual;
use super::types::ParetoSolution;

pub(super) fn compare_individuals(a: &Individual, b: &Individual) -> std::cmp::Ordering {
    a.rank
        .cmp(&b.rank)
        .then_with(|| b.crowding_distance.total_cmp(&a.crowding_distance))
        .then_with(|| sum_objectives(&a.objectives).total_cmp(&sum_objectives(&b.objectives)))
}

pub(super) fn compare_solutions(a: &ParetoSolution, b: &ParetoSolution) -> std::cmp::Ordering {
    a.rank
        .cmp(&b.rank)
        .then_with(|| b.crowding_distance.total_cmp(&a.crowding_distance))
        .then_with(|| sum_objectives(&a.objectives).total_cmp(&sum_objectives(&b.objectives)))
}
