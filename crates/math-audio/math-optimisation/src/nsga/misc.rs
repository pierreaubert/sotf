// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use ndarray::Array1;
use rand::Rng;
use rand::rngs::StdRng;

pub(super) fn random_in_bounds(bounds: &[(f64, f64)], rng: &mut StdRng) -> Array1<f64> {
    Array1::from(
        bounds
            .iter()
            .map(|(lo, hi)| {
                if hi > lo {
                    rng.random_range(*lo..=*hi)
                } else {
                    *lo
                }
            })
            .collect::<Vec<_>>(),
    )
}

pub(super) fn clamp_to_bounds(mut x: Array1<f64>, bounds: &[(f64, f64)]) -> Array1<f64> {
    for (xi, (lo, hi)) in x.iter_mut().zip(bounds.iter()) {
        *xi = xi.clamp(*lo, *hi);
    }
    x
}

pub(super) fn sbx_crossover(
    p1: &Array1<f64>,
    p2: &Array1<f64>,
    bounds: &[(f64, f64)],
    eta_c: f64,
    rng: &mut StdRng,
) -> (Array1<f64>, Array1<f64>) {
    let mut c1 = p1.clone();
    let mut c2 = p2.clone();
    for i in 0..p1.len() {
        if rng.random::<f64>() > 0.5 || (p1[i] - p2[i]).abs() <= 1e-14 {
            continue;
        }
        let u = rng
            .random::<f64>()
            .clamp(f64::MIN_POSITIVE, 1.0 - f64::EPSILON);
        let beta = if u <= 0.5 {
            (2.0 * u).powf(1.0 / (eta_c + 1.0))
        } else {
            (1.0 / (2.0 * (1.0 - u))).powf(1.0 / (eta_c + 1.0))
        };
        c1[i] = 0.5 * ((1.0 + beta) * p1[i] + (1.0 - beta) * p2[i]);
        c2[i] = 0.5 * ((1.0 - beta) * p1[i] + (1.0 + beta) * p2[i]);
        c1[i] = c1[i].clamp(bounds[i].0, bounds[i].1);
        c2[i] = c2[i].clamp(bounds[i].0, bounds[i].1);
    }
    (c1, c2)
}

pub(super) fn polynomial_mutation(
    x: &mut Array1<f64>,
    bounds: &[(f64, f64)],
    mutation_prob: f64,
    eta_m: f64,
    rng: &mut StdRng,
) {
    for i in 0..x.len() {
        if rng.random::<f64>() > mutation_prob {
            continue;
        }
        let (lo, hi) = bounds[i];
        let span = hi - lo;
        if span <= 0.0 {
            x[i] = lo;
            continue;
        }
        let y = x[i].clamp(lo, hi);
        let delta1 = (y - lo) / span;
        let delta2 = (hi - y) / span;
        let rnd = rng.random::<f64>();
        let mut_pow = 1.0 / (eta_m + 1.0);
        let deltaq = if rnd <= 0.5 {
            let xy = 1.0 - delta1;
            let val = 2.0 * rnd + (1.0 - 2.0 * rnd) * xy.powf(eta_m + 1.0);
            val.powf(mut_pow) - 1.0
        } else {
            let xy = 1.0 - delta2;
            let val = 2.0 * (1.0 - rnd) + 2.0 * (rnd - 0.5) * xy.powf(eta_m + 1.0);
            1.0 - val.powf(mut_pow)
        };
        x[i] = (y + deltaq * span).clamp(lo, hi);
    }
}

pub(super) fn dominates(a: &[f64], b: &[f64]) -> bool {
    if a.len() != b.len() || a.is_empty() {
        return false;
    }
    let mut strictly_better = false;
    for (&ai, &bi) in a.iter().zip(b.iter()) {
        if ai > bi {
            return false;
        }
        if ai < bi {
            strictly_better = true;
        }
    }
    strictly_better
}

pub(super) fn normalise_objectives(objectives: &[f64], ideal: &[f64], nadir: &[f64]) -> Vec<f64> {
    objectives
        .iter()
        .enumerate()
        .map(|(i, &v)| {
            let span = nadir[i] - ideal[i];
            if span > 0.0 && span.is_finite() {
                ((v - ideal[i]) / span).max(0.0)
            } else {
                0.0
            }
        })
        .collect()
}

pub(super) fn nearest_reference(point: &[f64], references: &[Vec<f64>]) -> (usize, f64) {
    references
        .iter()
        .enumerate()
        .map(|(idx, reference)| (idx, perpendicular_distance(point, reference)))
        .min_by(|a, b| a.1.total_cmp(&b.1))
        .unwrap_or((0, 0.0))
}

pub(super) fn perpendicular_distance(point: &[f64], reference: &[f64]) -> f64 {
    let ref_norm_sq = reference.iter().map(|r| r * r).sum::<f64>();
    if ref_norm_sq <= 0.0 {
        return point.iter().map(|v| v * v).sum::<f64>().sqrt();
    }
    let dot = point
        .iter()
        .zip(reference.iter())
        .map(|(p, r)| p * r)
        .sum::<f64>();
    let scale = dot / ref_norm_sq;
    point
        .iter()
        .zip(reference.iter())
        .map(|(p, r)| {
            let d = p - scale * r;
            d * d
        })
        .sum::<f64>()
        .sqrt()
}

pub(super) fn default_reference_partitions(m: usize) -> usize {
    match m {
        0 | 1 => 1,
        2 => 24,
        3 => 12,
        4 => 6,
        _ => 3,
    }
}

pub(super) fn sum_objectives(objectives: &[f64]) -> f64 {
    objectives.iter().sum::<f64>()
}
