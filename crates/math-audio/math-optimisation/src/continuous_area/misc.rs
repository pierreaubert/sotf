// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use crate::init_latin_hypercube::init_latin_hypercube;
use ndarray::{Array1, Array2};
use rand::SeedableRng;
use rand::rngs::StdRng;

pub(super) fn sobol_unit<const D: usize>(num_points: usize, _seed: u64) -> Vec<[f64; D]> {
    // Reuse the existing `init_sobol` implementation but re-package per-point.
    // `init_sobol` returns `Vec<Vec<f64>>` over user-supplied bounds; we
    // request `[0,1]^D` and copy into a stack array per point.
    let unit_bounds: Vec<(f64, f64)> = (0..D).map(|_| (0.0, 1.0)).collect();
    let raw = crate::init_sobol::init_halton(D, num_points, &unit_bounds);
    raw.into_iter()
        .map(|v| {
            let mut out = [0.0_f64; D];
            for (i, x) in v.into_iter().enumerate().take(D) {
                out[i] = x;
            }
            out
        })
        .collect()
}

pub(super) fn latin_hypercube_unit<const D: usize>(num_points: usize, seed: u64) -> Vec<[f64; D]> {
    let lower = Array1::<f64>::zeros(D);
    let upper = Array1::<f64>::ones(D);
    let is_free = vec![true; D];
    let mut rng = StdRng::seed_from_u64(seed);
    let m: Array2<f64> = init_latin_hypercube(D, num_points, &lower, &upper, &is_free, &mut rng);
    (0..num_points)
        .map(|row| {
            let mut out = [0.0_f64; D];
            for col in 0..D {
                out[col] = m[(row, col)];
            }
            out
        })
        .collect()
}

pub(super) fn standard_normal_cdf(x: f64) -> f64 {
    // 0.5 * (1 + erf(x / sqrt(2)))
    0.5 * (1.0 + erf(x / std::f64::consts::SQRT_2))
}

#[allow(clippy::excessive_precision)]
pub(super) fn inv_standard_normal(u: f64) -> f64 {
    // Beasley-Springer-Moro inverse normal CDF approximation (good to ~1e-7).
    // Constants are reference values from the published paper — keeping full
    // precision matters for round-trip accuracy at α/2 quantiles.
    // Clamp to (0, 1) to avoid ±inf at the tails.
    let u = u.clamp(1e-12, 1.0 - 1e-12);
    let a = [
        -3.969683028665376e+01,
        2.209460984245205e+02,
        -2.759285104469687e+02,
        1.383577518672690e+02,
        -3.066479806614716e+01,
        2.506628277459239e+00,
    ];
    let b = [
        -5.447609879822406e+01,
        1.615858368580409e+02,
        -1.556989798598866e+02,
        6.680131188771972e+01,
        -1.328068155288572e+01,
    ];
    let c = [
        -7.784894002430293e-03,
        -3.223964580411365e-01,
        -2.400758277161838e+00,
        -2.549732539343734e+00,
        4.374664141464968e+00,
        2.938163982698783e+00,
    ];
    let d = [
        7.784695709041462e-03,
        3.224671290700398e-01,
        2.445134137142996e+00,
        3.754408661907416e+00,
    ];

    let plow = 0.02425;
    let phigh = 1.0 - plow;

    if u < plow {
        let q = (-2.0 * u.ln()).sqrt();
        let num = ((((c[0] * q + c[1]) * q + c[2]) * q + c[3]) * q + c[4]) * q + c[5];
        let den = (((d[0] * q + d[1]) * q + d[2]) * q + d[3]) * q + 1.0;
        num / den
    } else if u <= phigh {
        let q = u - 0.5;
        let r = q * q;
        (((((a[0] * r + a[1]) * r + a[2]) * r + a[3]) * r + a[4]) * r + a[5]) * q
            / (((((b[0] * r + b[1]) * r + b[2]) * r + b[3]) * r + b[4]) * r + 1.0)
    } else {
        let q = (-2.0 * (1.0 - u).ln()).sqrt();
        let num = ((((c[0] * q + c[1]) * q + c[2]) * q + c[3]) * q + c[4]) * q + c[5];
        let den = (((d[0] * q + d[1]) * q + d[2]) * q + d[3]) * q + 1.0;
        -num / den
    }
}

pub(super) fn erf(x: f64) -> f64 {
    // Abramowitz & Stegun 7.1.26 — max error ~1.5e-7, plenty for prior transforms.
    let sign = x.signum();
    let x = x.abs();
    let a1 = 0.254829592;
    let a2 = -0.284496736;
    let a3 = 1.421413741;
    let a4 = -1.453152027;
    let a5 = 1.061405429;
    let p = 0.3275911;
    let t = 1.0 / (1.0 + p * x);
    let y = 1.0 - (((((a5 * t + a4) * t) + a3) * t + a2) * t + a1) * t * (-x * x).exp();
    sign * y
}
