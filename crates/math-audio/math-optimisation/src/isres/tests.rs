// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use super::individual::isres;
use super::isres_config::IsresConfig;
use super::misc::reflect_into_bounds;
use super::misc::standard_normal;
use super::types::IsresConstraint;
use crate::error::DEError;
use ndarray::{Array1, array};
use rand::SeedableRng;
use rand::rngs::StdRng;
use std::sync::Arc;

#[test]
fn sphere_unconstrained() {
    // f(x) = sum xi^2 — minimum at origin.
    let f = |x: &Array1<f64>| x.iter().map(|v| v * v).sum::<f64>();
    let cfg = IsresConfig {
        bounds: vec![(-5.0, 5.0); 4],
        mu: 20,
        lambda: 100,
        maxeval: 8_000,
        seed: Some(42),
        ..Default::default()
    };
    let report = isres(&f, &[], cfg).expect("isres failed");
    assert!(
        report.fun < 1e-3,
        "fun = {} should converge near 0",
        report.fun
    );
    for &xi in report.x.iter() {
        assert!(xi.abs() < 0.05, "xi = {} should be near 0", xi);
    }
}

#[test]
fn sphere_with_inequality() {
    // Minimize ||x||^2 subject to x[0] + x[1] >= 1
    // (i.e. constraint  1 - x[0] - x[1] <= 0).
    // Optimum at x = (0.5, 0.5, 0, ..., 0) with f = 0.5.
    let f = |x: &Array1<f64>| x.iter().map(|v| v * v).sum::<f64>();
    let constraints = vec![IsresConstraint {
        fun: Arc::new(|x: &Array1<f64>| 1.0 - x[0] - x[1]),
    }];
    let cfg = IsresConfig {
        bounds: vec![(-5.0, 5.0); 4],
        mu: 30,
        lambda: 200,
        maxeval: 30_000,
        seed: Some(7),
        ..Default::default()
    };
    let report = isres(&f, &constraints, cfg).expect("isres failed");
    assert!(report.feasible, "best should be feasible");
    // Generous tolerance — ISRES is stochastic and convergence on
    // small budgets isn't guaranteed to hit the analytical optimum.
    assert!(
        report.fun < 0.7,
        "fun = {} should converge near 0.5",
        report.fun
    );
}

#[test]
fn rejects_inverted_bounds() {
    let f = |x: &Array1<f64>| x[0] * x[0];
    let cfg = IsresConfig {
        bounds: vec![(1.0, -1.0)],
        mu: 10,
        lambda: 30,
        maxeval: 100,
        ..Default::default()
    };
    let err = isres(&f, &[], cfg).unwrap_err();
    matches!(err, DEError::InvalidBounds { .. });
}

#[test]
fn rejects_tiny_population() {
    let f = |x: &Array1<f64>| x[0] * x[0];
    let cfg = IsresConfig {
        bounds: vec![(-1.0, 1.0)],
        mu: 1,
        lambda: 10,
        maxeval: 100,
        ..Default::default()
    };
    let err = isres(&f, &[], cfg).unwrap_err();
    matches!(err, DEError::PopulationTooSmall { .. });
}

#[test]
fn deterministic_with_seed() {
    let f = |x: &Array1<f64>| x.iter().map(|v| v * v).sum::<f64>();
    let cfg1 = IsresConfig {
        bounds: vec![(-5.0, 5.0); 3],
        mu: 15,
        lambda: 80,
        maxeval: 2_000,
        seed: Some(123),
        ..Default::default()
    };
    let cfg2 = cfg1.clone();
    let r1 = isres(&f, &[], cfg1).unwrap();
    let r2 = isres(&f, &[], cfg2).unwrap();
    assert!((r1.fun - r2.fun).abs() < 1e-12, "seeded runs must match");
}

#[test]
fn test_reflect_into_bounds_inside() {
    assert_eq!(reflect_into_bounds(0.5, 0.0, 1.0), 0.5);
}

#[test]
fn test_reflect_into_bounds_reflect_low() {
    assert_eq!(reflect_into_bounds(-0.3, 0.0, 1.0), 0.3);
}

#[test]
fn test_reflect_into_bounds_reflect_high() {
    assert!((reflect_into_bounds(1.4, 0.0, 1.0) - 0.6).abs() < 1e-12);
}

#[test]
fn test_reflect_into_bounds_overshoot_to_mid() {
    assert_eq!(reflect_into_bounds(-1.5, 0.0, 1.0), 0.5);
    assert_eq!(reflect_into_bounds(2.5, 0.0, 1.0), 0.5);
}

#[test]
fn test_reflect_into_bounds_nonfinite() {
    assert_eq!(reflect_into_bounds(f64::NAN, 0.0, 1.0), 0.5);
}

#[test]
fn test_standard_normal_samples_are_finite() {
    let mut rng = StdRng::seed_from_u64(1);
    for _ in 0..20 {
        assert!(standard_normal(&mut rng).is_finite());
    }
}

#[test]
fn test_empty_bounds_rejected() {
    let f = |x: &Array1<f64>| x.iter().map(|v| v * v).sum::<f64>();
    let cfg = IsresConfig {
        bounds: vec![],
        mu: 10,
        lambda: 50,
        maxeval: 100,
        ..Default::default()
    };
    let err = isres(&f, &[], cfg).unwrap_err();
    assert!(matches!(err, DEError::BoundsMismatch { .. }));
}

#[test]
fn test_multiple_constraints() {
    let f = |x: &Array1<f64>| x.iter().map(|v| v * v).sum::<f64>();
    let constraints = vec![
        IsresConstraint {
            fun: Arc::new(|x: &Array1<f64>| x[0] - 1.0), // x0 <= 1
        },
        IsresConstraint {
            fun: Arc::new(|x: &Array1<f64>| -x[1] - 1.0), // x1 >= -1
        },
    ];
    let cfg = IsresConfig {
        bounds: vec![(-5.0, 5.0); 2],
        mu: 20,
        lambda: 100,
        maxeval: 10_000,
        seed: Some(42),
        ..Default::default()
    };
    let report = isres(&f, &constraints, cfg).expect("isres failed");
    assert!(
        report.max_violation <= 0.1 || report.feasible,
        "Should be near feasible: max_violation={}, feasible={}",
        report.max_violation,
        report.feasible
    );
}

#[test]
fn test_x0_seeding() {
    let f = |x: &Array1<f64>| (x[0] - 2.0).powi(2) + (x[1] + 1.0).powi(2);
    let cfg = IsresConfig {
        bounds: vec![(-5.0, 5.0); 2],
        x0: Some(array![2.0, -1.0]),
        mu: 10,
        lambda: 50,
        maxeval: 2_000,
        seed: Some(7),
        ..Default::default()
    };
    let report = isres(&f, &[], cfg).expect("isres failed");
    assert!(
        report.fun < 1.0,
        "Should converge near seeded optimum: f={}",
        report.fun
    );
}

#[test]
fn test_maxeval_reached() {
    let f = |x: &Array1<f64>| x.iter().map(|v| v * v).sum::<f64>();
    let cfg = IsresConfig {
        bounds: vec![(-5.0, 5.0); 2],
        mu: 5,
        lambda: 20,
        maxeval: 50,
        seed: Some(1),
        ..Default::default()
    };
    let report = isres(&f, &[], cfg).expect("isres failed");
    assert!(
        report.nfev <= 60,
        "nfev should respect maxeval: {}",
        report.nfev
    );
}

#[test]
fn test_stagnation_termination() {
    let f = |x: &Array1<f64>| x.iter().map(|v| v * v).sum::<f64>();
    let cfg = IsresConfig {
        bounds: vec![(-5.0, 5.0); 2],
        mu: 10,
        lambda: 50,
        maxeval: 100_000,
        f_tol: 1e-3,
        stagnation_window: 5,
        seed: Some(42),
        ..Default::default()
    };
    let report = isres(&f, &[], cfg).expect("isres failed");
    assert!(
        report.fun < 1.0,
        "Should converge with stagnation termination: f={}",
        report.fun
    );
}

#[test]
fn test_invalid_lambda_less_than_mu() {
    let f = |x: &Array1<f64>| x[0] * x[0];
    let cfg = IsresConfig {
        bounds: vec![(-1.0, 1.0)],
        mu: 10,
        lambda: 5,
        maxeval: 100,
        ..Default::default()
    };
    let err = isres(&f, &[], cfg).unwrap_err();
    assert!(matches!(err, DEError::PopulationTooSmall { .. }));
}
