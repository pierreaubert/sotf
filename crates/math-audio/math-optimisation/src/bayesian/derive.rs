// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use super::bayes_opt_config::BayesOptConfig;
use super::consts::MAX_LENGTHSCALE;
use super::consts::MIN_LENGTHSCALE;
use crate::error::{DEError, Result};

pub(super) fn derive_initial_samples(n: usize, config: &BayesOptConfig) -> usize {
    if config.initial_samples == 0 {
        (2 * n + 1).max(8)
    } else {
        config.initial_samples
    }
}

pub(super) fn derive_candidate_pool_size(n: usize, config: &BayesOptConfig) -> usize {
    if config.candidate_pool_size == 0 {
        (64 * n).max(512)
    } else {
        config.candidate_pool_size.max(16)
    }
}

pub(super) fn derive_lengthscales(n: usize, config: &BayesOptConfig) -> Result<Vec<f64>> {
    if let Some(ref ls) = config.lengthscales {
        if ls.len() != n {
            return Err(DEError::InvalidConfig {
                message: format!(
                    "lengthscales dimension mismatch: expected {}, got {}",
                    n,
                    ls.len()
                ),
            });
        }
        Ok(ls
            .iter()
            .map(|v| v.clamp(MIN_LENGTHSCALE, MAX_LENGTHSCALE))
            .collect())
    } else {
        Ok(vec![0.25; n])
    }
}
