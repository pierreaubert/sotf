// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use super::benchmark_comparison::BenchmarkComparison;
use super::benchmark_config::build_de_config;
use super::benchmark_config::failed_result;
use super::benchmark_config::solution_within_tolerances;
use super::benchmark_config::validate_solution;
use super::benchmark_result::BenchmarkResult;
use super::cmaes::cmaes_eval_budget;
use super::cmaes::cmaes_restart_seed;
use super::function_registry::FUNCTION_REGISTRY;
use super::optimizer_kind::OptimizerKind;
use super::types::BenchmarkConfig;
use super::types::OptimizerOutcome;
use math_audio_optimisation::{CmaEsConfig, cma_es, run_recorded_differential_evolution};
use ndarray::Array1;
use std::time::{Duration, Instant};

pub(super) fn run_benchmark_comparison(config: &BenchmarkConfig) -> BenchmarkComparison {
    let Some(function) = FUNCTION_REGISTRY.get(&config.function_name) else {
        let message = format!("Function {} not found in registry", config.function_name);
        return BenchmarkComparison {
            name: config.name.clone(),
            de: failed_result(
                config,
                OptimizerKind::De,
                Duration::from_secs(0),
                message.clone(),
            ),
            cmaes: failed_result(
                config,
                OptimizerKind::CmaEs,
                Duration::from_secs(0),
                message,
            ),
        };
    };

    let de = match build_de_config(config) {
        Ok(de_config) => run_de_benchmark(config, function, de_config),
        Err(e) => failed_result(
            config,
            OptimizerKind::De,
            Duration::from_secs(0),
            format!("Config error: {}", e),
        ),
    };
    let cmaes = run_cmaes_benchmark(config, function);

    BenchmarkComparison {
        name: config.name.clone(),
        de,
        cmaes,
    }
}

/// Run a DE benchmark with the given parameters and validate results.
pub(super) fn run_de_benchmark(
    config: &BenchmarkConfig,
    function: fn(&Array1<f64>) -> f64,
    de_config: math_audio_optimisation::DEConfig,
) -> BenchmarkResult {
    let start_time = Instant::now();

    let result =
        run_recorded_differential_evolution(&config.name, function, &config.bounds, de_config);
    let duration = start_time.elapsed();

    match result {
        Ok((report, _csv_path)) => validate_solution(
            config,
            OptimizerKind::De,
            &report.x,
            report.fun,
            report.nfev,
            duration,
        ),
        Err(e) => failed_result(
            config,
            OptimizerKind::De,
            duration,
            format!("Optimization failed: {}", e),
        ),
    }
}

/// Run a CMA-ES benchmark against the same evaluation budget as the DE config.
pub(super) fn run_cmaes_benchmark(
    config: &BenchmarkConfig,
    function: fn(&Array1<f64>) -> f64,
) -> BenchmarkResult {
    let start_time = Instant::now();
    let result = run_budgeted_cmaes(config, function);
    let duration = start_time.elapsed();

    match result {
        Ok(outcome) => validate_solution(
            config,
            OptimizerKind::CmaEs,
            &outcome.x,
            outcome.fun,
            outcome.nfev,
            duration,
        ),
        Err(e) => failed_result(
            config,
            OptimizerKind::CmaEs,
            duration,
            format!("Optimization failed: {e}"),
        ),
    }
}

pub(super) fn run_budgeted_cmaes(
    config: &BenchmarkConfig,
    function: fn(&Array1<f64>) -> f64,
) -> Result<OptimizerOutcome, String> {
    let mut remaining = cmaes_eval_budget(config);
    let mut total_nfev = 0usize;
    let mut restart = 0usize;
    let mut best_x = None;
    let mut best_fun = f64::INFINITY;

    while remaining > 0 {
        let cmaes_config = CmaEsConfig {
            bounds: config.bounds.clone(),
            maxeval: remaining,
            seed: Some(cmaes_restart_seed(config.seed, restart)),
            target_f: config.fun_tolerance,
            ..Default::default()
        };

        let report = cma_es(&function, cmaes_config).map_err(|e| format!("{e}"))?;
        let consumed = report.nfev.max(1);
        total_nfev = total_nfev.saturating_add(report.nfev);
        remaining = remaining.saturating_sub(consumed);

        if report.fun < best_fun {
            best_fun = report.fun;
            best_x = Some(report.x.clone());
        }
        if solution_within_tolerances(config, &report.x, report.fun) {
            break;
        }

        restart = restart.saturating_add(1);
    }

    let Some(x) = best_x else {
        return Err("CMA-ES produced no result".to_string());
    };

    Ok(OptimizerOutcome {
        x,
        fun: best_fun,
        nfev: total_nfev,
    })
}
