// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use super::optimizer_kind::OptimizerKind;
use std::fmt;
use std::time::Duration;

#[derive(Debug, Clone)]
pub(super) struct BenchmarkResult {
    pub(super) name: String,
    pub(super) optimizer: OptimizerKind,
    pub(super) success: bool,
    pub(super) fun_value: f64,
    pub(super) fun_tolerance: f64,
    pub(super) position_errors: Vec<f64>,
    pub(super) position_tolerance: f64,
    pub(super) nfev: usize,
    pub(super) duration: Duration,
    pub(super) error_message: Option<String>,
}

impl fmt::Display for BenchmarkResult {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        let status = if self.success { "✅ PASS" } else { "❌ FAIL" };
        write!(
            f,
            "{} {:<6} {} (fun: {:.6e} < {:.2e}, pos_errs: max {:.6} < {:.2}, nfev: {}, time: {:.2}s)",
            status,
            self.optimizer.label(),
            self.name,
            self.fun_value,
            self.fun_tolerance,
            self.position_errors.iter().fold(0.0f64, |a, &b| a.max(b)),
            self.position_tolerance,
            self.nfev,
            self.duration.as_secs_f64()
        )?;
        if let Some(ref err) = self.error_message {
            write!(f, " - {}", err)?;
        }
        Ok(())
    }
}

pub(super) fn status_label(result: &BenchmarkResult) -> &'static str {
    if result.success {
        "✅ PASS"
    } else {
        "❌ FAIL"
    }
}
