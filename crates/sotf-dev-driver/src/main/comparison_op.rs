// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use anyhow::{Result, anyhow};

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub(super) enum ComparisonOp {
    Eq,
    Ne,
    Gt,
    Ge,
    Lt,
    Le,
}

impl ComparisonOp {
    pub(super) fn as_str(self) -> &'static str {
        match self {
            Self::Eq => "==",
            Self::Ne => "!=",
            Self::Gt => ">",
            Self::Ge => ">=",
            Self::Lt => "<",
            Self::Le => "<=",
        }
    }
}

pub(super) fn split_comparison(core: &str) -> Result<(&str, ComparisonOp, &str)> {
    for (needle, op) in [
        (">=", ComparisonOp::Ge),
        ("<=", ComparisonOp::Le),
        ("!=", ComparisonOp::Ne),
        ("==", ComparisonOp::Eq),
        (">", ComparisonOp::Gt),
        ("<", ComparisonOp::Lt),
    ] {
        if let Some((path, lit_text)) = core.split_once(needle) {
            return Ok((path, op, lit_text));
        }
    }
    Err(anyhow!("missing comparison operator"))
}
