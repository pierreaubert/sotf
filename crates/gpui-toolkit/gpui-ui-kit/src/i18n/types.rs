// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use super::i18n_ext::I18nExt;
use super::i18n_state::I18nState;
use super::language::Language;
use gpui::App;

/// Translation keys
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum TranslationKey {
    // App
    AppTitle,
    AppSubtitle,

    // Menu
    MenuFile,
    MenuEdit,
    MenuView,
    MenuHelp,
    MenuQuit,
    MenuTheme,
    MenuLanguage,
    MenuSettings,

    // Theme
    ThemeDark,
    ThemeLight,

    // Section titles
    SectionButtons,
    SectionTypography,
    SectionBadges,
    SectionAvatars,
    SectionFormControls,
    SectionProgress,
    SectionAlerts,
    SectionTabs,
    SectionCards,
    SectionBreadcrumbs,
    SectionSpinners,
    SectionLayout,
    SectionIconButtons,
    SectionToasts,
    SectionDialogs,
    SectionMenus,
    SectionTooltips,
    SectionPotentiometers,
    SectionAccordion,
    SectionQrCode,
    SectionContextMenu,
    SectionPopover,
    SectionSidebar,
    SectionStatusBar,
    SectionSearchBar,
    SectionKeyboardShortcut,
    SectionEmptyState,
    SectionConfirmDialog,
    SectionSplitPane,
    SectionImageView,
    SectionSettingsForm,
    SectionStepIndicator,
    SectionLoadingOverlay,
    SectionTag,
    SectionToolbar,
    SectionNotification,
    SectionTreeView,
    SectionDragList,
    SectionCommandPalette,
    SectionAccessibility,

    // Component labels
    LabelVariants,
    LabelSizes,
    LabelStates,
    LabelToggles,
    LabelCheckboxes,
    LabelSlider,
    LabelInput,
    LabelSmall,
    LabelMedium,
    LabelLarge,
    LabelDisabled,
    LabelSelected,

    // Button labels
    ButtonPrimary,
    ButtonSecondary,
    ButtonDestructive,
    ButtonGhost,
    ButtonOutline,
    ButtonCancel,
    ButtonSave,
    ButtonConfirm,

    // Dialog
    DialogConfirmTitle,
    DialogConfirmMessage,

    // Alerts
    AlertInfo,
    AlertSuccess,
    AlertWarning,
    AlertError,
    AlertInfoMessage,
    AlertSuccessMessage,
    AlertWarningMessage,
    AlertErrorMessage,

    // Accordion
    AccordionGettingStarted,
    AccordionFeatures,
    AccordionConfiguration,
}

impl I18nExt for App {
    fn t(&self, key: TranslationKey) -> &'static str {
        self.try_global::<I18nState>()
            .map(|s| s.t(key))
            .unwrap_or("???")
    }

    fn language(&self) -> Language {
        self.try_global::<I18nState>()
            .map(|s| s.language)
            .unwrap_or_default()
    }
}
