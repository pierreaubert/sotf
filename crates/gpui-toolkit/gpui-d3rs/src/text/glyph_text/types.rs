// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use super::blend::blend_raw_pixel;
use gpui::SharedString;
use std::hash::Hash;

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum HorizontalTextAnchor {
    Start,
    Middle,
    End,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum VerticalTextAnchor {
    Top,
    Middle,
    Alphabetic,
    Bottom,
}

#[derive(Debug, Clone, Copy)]
pub struct GlyphTextMetrics {
    pub width: f32,
    pub height: f32,
    pub ascent: f32,
    pub descent: f32,
}

#[derive(Debug, Clone)]
pub(super) struct RasterText {
    pub(super) width: u32,
    pub(super) height: u32,
    pub(super) pixels: Vec<u8>,
    pub(super) paint_offset: [f32; 2],
    pub(super) anchor: [f32; 2],
}

#[derive(Debug, Clone, Copy)]
pub(super) struct RawMetrics {
    pub(super) width: f32,
    pub(super) ink_min_x: f32,
    pub(super) ink_max_x: f32,
    pub(super) min_y: f32,
    pub(super) max_y: f32,
}

#[derive(Debug, Clone)]
pub(super) struct ChartTextLayout {
    pub(super) svg: String,
    pub(super) cache_key: SharedString,
    pub(super) width: f32,
    pub(super) height: f32,
    pub(super) anchor: [f32; 2],
}

#[derive(Debug, Clone, Copy)]
pub(super) struct LogicalTextBox {
    pub(super) width: f32,
    pub(super) height: f32,
    pub(super) baseline_y: f32,
    pub(super) anchor: [f32; 2],
}

pub(super) fn horizontal_anchor_offset(width: f32, anchor: HorizontalTextAnchor) -> f32 {
    match anchor {
        HorizontalTextAnchor::Start => 0.0,
        HorizontalTextAnchor::Middle => width / 2.0,
        HorizontalTextAnchor::End => width,
    }
}

pub(super) fn vertical_anchor_offset(raw: &RawMetrics, anchor: VerticalTextAnchor) -> f32 {
    match anchor {
        VerticalTextAnchor::Top => 0.0,
        VerticalTextAnchor::Middle => (raw.max_y - raw.min_y) / 2.0,
        VerticalTextAnchor::Alphabetic => -raw.min_y,
        VerticalTextAnchor::Bottom => raw.max_y - raw.min_y,
    }
}

pub(super) fn rotate_raster(src: &RasterText, rotation: f32) -> RasterText {
    let cos_r = rotation.cos();
    let sin_r = rotation.sin();
    let src_w = src.width as f32;
    let src_h = src.height as f32;
    let corners = [
        (-src_w / 2.0, -src_h / 2.0),
        (src_w / 2.0, -src_h / 2.0),
        (-src_w / 2.0, src_h / 2.0),
        (src_w / 2.0, src_h / 2.0),
    ];

    let mut min_x = f32::INFINITY;
    let mut max_x = f32::NEG_INFINITY;
    let mut min_y = f32::INFINITY;
    let mut max_y = f32::NEG_INFINITY;

    for (x, y) in corners {
        let rx = x * cos_r - y * sin_r;
        let ry = x * sin_r + y * cos_r;
        min_x = min_x.min(rx);
        max_x = max_x.max(rx);
        min_y = min_y.min(ry);
        max_y = max_y.max(ry);
    }

    let dst_w = (max_x - min_x).ceil().max(1.0) as u32 + 2;
    let dst_h = (max_y - min_y).ceil().max(1.0) as u32 + 2;
    let mut dst = vec![0u8; (dst_w * dst_h * 4) as usize];
    let dst_cx = dst_w as f32 / 2.0;
    let dst_cy = dst_h as f32 / 2.0;
    let src_cx = src_w / 2.0;
    let src_cy = src_h / 2.0;

    for y in 0..src.height {
        for x in 0..src.width {
            let idx = ((y * src.width + x) * 4) as usize;
            let alpha = src.pixels[idx + 3];
            if alpha == 0 {
                continue;
            }

            let lx = x as f32 + 0.5 - src_cx;
            let ly = y as f32 + 0.5 - src_cy;
            let rx = lx * cos_r - ly * sin_r;
            let ry = lx * sin_r + ly * cos_r;
            let dx = (dst_cx + rx).round() as i32;
            let dy = (dst_cy + ry).round() as i32;
            blend_raw_pixel(&mut dst, dst_w, dst_h, dx, dy, &src.pixels[idx..idx + 4]);
        }
    }

    let anchor_lx = src.anchor[0] - src_cx;
    let anchor_ly = src.anchor[1] - src_cy;
    let anchor_rx = anchor_lx * cos_r - anchor_ly * sin_r;
    let anchor_ry = anchor_lx * sin_r + anchor_ly * cos_r;

    RasterText {
        width: dst_w,
        height: dst_h,
        pixels: dst,
        paint_offset: [0.0, 0.0],
        anchor: [dst_cx + anchor_rx, dst_cy + anchor_ry],
    }
}
