// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use super::consts::ISOLINE_OCCLUSION_SAMPLE_PX;
use super::projected_depth_buffer::ProjectedDepthBuffer;
use crate::shape::contour_smoothing::StrokePoint;
use glam::Vec3;
use gpui::*;

pub(super) fn paint_stroke_segment(
    segment: &[StrokePoint],
    bounds: Bounds<Pixels>,
    width_px: f32,
    color: Rgba,
    window: &mut Window,
) {
    if segment.len() < 2 {
        return;
    }

    let mut builder = PathBuilder::stroke(px(width_px));
    builder.move_to(Point {
        x: px(segment[0].x) + bounds.origin.x,
        y: px(segment[0].y) + bounds.origin.y,
    });
    for point in &segment[1..] {
        builder.line_to(Point {
            x: px(point.x) + bounds.origin.x,
            y: px(point.y) + bounds.origin.y,
        });
    }
    if let Ok(path) = builder.build() {
        window.paint_path(path, color);
    }
}

pub(super) fn paint_depth_clipped_stroke_segment(
    start: Vec3,
    end: Vec3,
    depth_buffer: &ProjectedDepthBuffer,
    bounds: Bounds<Pixels>,
    width_px: f32,
    color: Rgba,
    window: &mut Window,
) {
    let screen_length = (end - start).truncate().length();
    let steps = (screen_length / ISOLINE_OCCLUSION_SAMPLE_PX)
        .ceil()
        .max(1.0) as usize;
    let mut visible_run = Vec::new();

    for step in 0..=steps {
        let t = step as f32 / steps as f32;
        let point = start.lerp(end, t);
        if depth_buffer.is_visible(point) {
            visible_run.push(StrokePoint::new(point.x, point.y));
        } else {
            if visible_run.len() >= 2 {
                paint_stroke_segment(&visible_run, bounds, width_px, color, window);
            }
            visible_run.clear();
        }
    }

    if visible_run.len() >= 2 {
        paint_stroke_segment(&visible_run, bounds, width_px, color, window);
    }
}
