// Auto-generated by r2factor. Manual edits will be overwritten on next split.

#[doc = " Integration tests ported from the TypeScript pretext test suite."]
#[doc = ""]
#[doc = " Uses a deterministic \"fake canvas\" measure that mimics the original test's"]
#[doc = " character-width model:"]
#[doc = " - Space: 0.33× font_size (compressible)"]
#[doc = " - Tab: 1.32× font_size"]
#[doc = " - Punctuation (.,!?:;()[]{}\"'): 0.4× font_size"]
#[doc = " - Regular characters: 0.6× font_size"]
use gpui_pretext::TextMeasure;

pub(super) struct FakeMeasure {
    pub(super) font_size: f64,
}

impl FakeMeasure {
    pub(super) fn new(font_size: f64) -> Self {
        Self { font_size }
    }
}

impl TextMeasure for FakeMeasure {
    fn measure_width(&self, text: &str) -> f64 {
        let mut width = 0.0;
        for ch in text.chars() {
            width += match ch {
                ' ' => 0.33 * self.font_size,
                '\t' => 1.32 * self.font_size,
                '.' | ',' | '!' | '?' | ':' | ';' | '(' | ')' | '[' | ']' | '{' | '}' | '"'
                | '\'' | '-' => 0.4 * self.font_size,
                _ => 0.6 * self.font_size,
            };
        }
        width
    }
}
