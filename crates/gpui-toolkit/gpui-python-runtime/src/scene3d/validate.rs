// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use crate::error::Scene3DError;

pub(super) fn validate_id(id: &str, field: &'static str) -> Result<(), Scene3DError> {
    if id.trim().is_empty() {
        return Err(Scene3DError::InvalidData {
            field,
            reason: "must not be empty",
        });
    }
    Ok(())
}

pub(super) fn validate_axis(
    values: &[f64],
    expected_len: usize,
    field: &'static str,
    expected_field: &'static str,
    log: bool,
) -> Result<(), Scene3DError> {
    if values.len() != expected_len {
        return Err(Scene3DError::DataLengthMismatch {
            x_field: field,
            y_field: expected_field,
            x_len: values.len(),
            y_len: expected_len,
        });
    }
    validate_finite_f64_slice(values, field)?;
    validate_monotonic(values, field)?;
    if log {
        validate_positive_f64_slice(values, field)?;
    }
    Ok(())
}

pub(super) fn validate_finite_f32(value: f32, field: &'static str) -> Result<(), Scene3DError> {
    if !value.is_finite() {
        return Err(Scene3DError::InvalidData {
            field,
            reason: "contains NaN or Infinity",
        });
    }
    Ok(())
}

pub(super) fn validate_positive_f32(value: f32, field: &'static str) -> Result<(), Scene3DError> {
    validate_finite_f32(value, field)?;
    if value <= 0.0 {
        return Err(Scene3DError::InvalidData {
            field,
            reason: "must be positive",
        });
    }
    Ok(())
}

pub(super) fn validate_finite_f64_slice(
    values: &[f64],
    field: &'static str,
) -> Result<(), Scene3DError> {
    if values.is_empty() {
        return Err(Scene3DError::EmptyData { field });
    }
    if values.iter().any(|value| !value.is_finite()) {
        return Err(Scene3DError::InvalidData {
            field,
            reason: "contains NaN or Infinity",
        });
    }
    Ok(())
}

pub(super) fn validate_positive_f64_slice(
    values: &[f64],
    field: &'static str,
) -> Result<(), Scene3DError> {
    if values.iter().any(|value| *value <= 0.0) {
        return Err(Scene3DError::InvalidData {
            field,
            reason: "contains non-positive values for log scale",
        });
    }
    Ok(())
}

pub(super) fn validate_monotonic(values: &[f64], field: &'static str) -> Result<(), Scene3DError> {
    for window in values.windows(2) {
        if window[1] <= window[0] {
            return Err(Scene3DError::InvalidData {
                field,
                reason: "must be strictly monotonically increasing",
            });
        }
    }
    Ok(())
}
