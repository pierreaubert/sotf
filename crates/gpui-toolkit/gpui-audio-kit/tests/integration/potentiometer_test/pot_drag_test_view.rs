// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use gpui::{Context, Window, div, prelude::*};
use gpui_audio_kit::audio::potentiometer::Potentiometer;
use std::cell::RefCell;
use std::rc::Rc;
use std::sync::Arc;
use std::sync::atomic::{AtomicBool, Ordering};

/// View that tracks drag start events
pub(super) struct PotDragTestView {
    pub(super) drag_started: Arc<AtomicBool>,
    pub(super) drag_y: Rc<RefCell<Option<f32>>>,
}

impl Render for PotDragTestView {
    fn render(&mut self, _window: &mut Window, _cx: &mut Context<Self>) -> impl IntoElement {
        let drag_started = self.drag_started.clone();
        let drag_y = self.drag_y.clone();

        div().size_full().child(
            Potentiometer::new("drag-test-pot")
                .value(50.0)
                .min(0.0)
                .max(100.0)
                .label("Draggable")
                .on_drag_start(move |y, _value, _window, _cx| {
                    drag_started.store(true, Ordering::SeqCst);
                    *drag_y.borrow_mut() = Some(y);
                }),
        )
    }
}
