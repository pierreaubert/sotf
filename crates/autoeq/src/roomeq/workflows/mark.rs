// Auto-generated by r2factor. Manual edits will be overwritten on next split.

pub(super) fn mark_route_owned_plugin(
    mut plugin: super::super::types::PluginConfigWrapper,
) -> super::super::types::PluginConfigWrapper {
    if let Some(params) = plugin.parameters.as_object_mut() {
        params.insert(
            "room_eq_stage".to_string(),
            serde_json::json!("route_owned"),
        );
        params.insert(
            "label".to_string(),
            serde_json::json!("room_eq_route_owned"),
        );
    }
    plugin
}

pub(super) fn mark_plugin_stage(
    mut plugin: super::super::types::PluginConfigWrapper,
    stage: &str,
) -> super::super::types::PluginConfigWrapper {
    if let Some(params) = plugin.parameters.as_object_mut() {
        params.insert("room_eq_stage".to_string(), serde_json::json!(stage));
    }
    plugin
}

pub(super) fn mark_plugins_stage(
    plugins: Vec<super::super::types::PluginConfigWrapper>,
    stage: &str,
) -> Vec<super::super::types::PluginConfigWrapper> {
    plugins
        .into_iter()
        .map(|plugin| mark_plugin_stage(plugin, stage))
        .collect()
}
