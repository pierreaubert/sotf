// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use autoeq::roomeq::RoomConfig;

#[derive(Debug, Clone, Copy)]
#[allow(dead_code)]
pub(super) struct DifficultyLevel {
    pub(super) name: &'static str,
    pub(super) modes: &'static [(f64, f64, f64)], // (freq, Q, gain_db)
    pub(super) noise_rms: f64,
    pub(super) recovery_factor: f64,
}

#[derive(Debug, Clone)]
pub(super) struct MultiSubDifficulty {
    pub(super) name: &'static str,
    pub(super) n_subs: usize,
    /// (freq, Q, gain_db) — shared room modes affecting all subs
    pub(super) shared_modes: &'static [(f64, f64, f64)],
    /// Per-sub unique modes — outer len must be 0 or n_subs
    pub(super) per_sub_modes: &'static [&'static [(f64, f64, f64)]],
    /// Per-sub propagation delay in ms
    pub(super) delays_ms: &'static [f64],
    pub(super) noise_rms: f64,
}

/// Multi-sub topology variant
#[derive(Debug, Clone, Copy)]
pub(super) struct MultiSubTopology {
    pub(super) name: &'static str,
    pub(super) allpass: bool,
}

/// LFE sub-topology for multi-channel tests
#[derive(Debug, Clone, Copy)]
pub(super) struct SubTopology {
    pub(super) name: &'static str,
}

#[derive(Debug, Clone)]
pub(super) struct OptionDef {
    pub(super) name: &'static str,
    pub(super) apply: fn(&mut RoomConfig),
}

#[derive(Debug)]
#[allow(dead_code)]
pub(super) struct TestResult {
    pub(super) name: String,
    pub(super) passed: bool,
    pub(super) pre_score: f64,
    pub(super) post_score: f64,
    pub(super) epa_preference: Option<f64>,
    pub(super) reason: String,
}
