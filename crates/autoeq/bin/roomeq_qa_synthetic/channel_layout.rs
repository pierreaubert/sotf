// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use super::consts::ALL_SUB_TOPOS;
use super::types::SubTopology;
use autoeq::roomeq::SystemModel;

#[derive(Debug, Clone, Copy)]
pub(super) struct ChannelLayout {
    pub(super) name: &'static str,
    pub(super) mains: &'static [&'static str],
    pub(super) has_lfe: bool,
    pub(super) heights: &'static [&'static str],
}

impl ChannelLayout {
    pub(super) fn system_model(&self) -> SystemModel {
        if self.mains.len() <= 2 && self.heights.is_empty() {
            SystemModel::Stereo
        } else {
            SystemModel::HomeCinema
        }
    }
}

/// Get applicable sub topologies for a layout
pub(super) fn sub_topos_for_layout(layout: &ChannelLayout) -> &'static [SubTopology] {
    if !layout.has_lfe {
        &[] // no LFE → no sub topology dimension, just mains
    } else {
        ALL_SUB_TOPOS
    }
}
