// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use std::collections::{HashMap, VecDeque};
use std::path::PathBuf;

pub(super) fn stream_queue_album(stream: &sotf_audio_player::SavedStream) -> sotf_audio_player::Album {
    sotf_audio_player::Album {
        title: stream.name.clone(),
        tracks: vec![sotf_audio_player::Track {
            path: PathBuf::from(&stream.url),
            source: Some(stream.audio_source()),
            title: Some(stream.name.clone()),
            artist: Some("Streams".to_string()),
            duration_secs: None,
            ..Default::default()
        }],
        ..Default::default()
    }
}

pub(super) fn touch_lru<T: Clone + Eq>(lru: &mut VecDeque<T>, key: T) {
    lru.retain(|existing| existing != &key);
    lru.push_back(key);
}

pub(super) fn evict_lru<T: Clone + Eq + std::hash::Hash, V>(
    values: &mut HashMap<T, V>,
    lru: &mut VecDeque<T>,
    max_len: usize,
) {
    while values.len() > max_len {
        let Some(key) = lru.pop_front() else {
            break;
        };
        values.remove(&key);
    }
}

pub(super) fn current_minutes_after_midnight() -> u16 {
    use chrono::Timelike;

    let now = chrono::Local::now();
    (now.hour() * 60 + now.minute()) as u16
}

