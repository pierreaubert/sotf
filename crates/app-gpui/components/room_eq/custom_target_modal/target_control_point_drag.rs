// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use super::consts::CONTROL_POINT_RADIUS;
use gpui::prelude::*;
use gpui::*;
use gpui_ui_kit::theme::ThemeExt;

/// Drag data for control point manipulation
#[derive(Clone)]
pub(super) struct TargetControlPointDrag {
    pub(super) point_idx: usize,
    #[allow(dead_code)]
    pub(super) start_freq: f64,
    #[allow(dead_code)]
    pub(super) start_level: f64,
}

impl Render for TargetControlPointDrag {
    fn render(&mut self, _window: &mut Window, cx: &mut Context<Self>) -> impl IntoElement {
        let theme = cx.theme();
        div()
            .w(px(CONTROL_POINT_RADIUS * 2.0))
            .h(px(CONTROL_POINT_RADIUS * 2.0))
            .rounded_full()
            .bg(theme.accent)
            .border(px(2.0))
            .border_color(theme.text_on_accent)
            .shadow_lg()
    }
}
