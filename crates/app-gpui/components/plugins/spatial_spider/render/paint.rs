// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use gpui::*;

pub(super) fn paint_filled_rect(window: &mut Window, bounds: Bounds<Pixels>, color: Rgba) {
    let o = bounds.origin;
    let r = Point {
        x: o.x + bounds.size.width,
        y: o.y + bounds.size.height,
    };
    let mut b = PathBuilder::fill();
    b.move_to(Point { x: o.x, y: o.y });
    b.line_to(Point { x: r.x, y: o.y });
    b.line_to(Point { x: r.x, y: r.y });
    b.line_to(Point { x: o.x, y: r.y });
    b.line_to(Point { x: o.x, y: o.y });
    if let Ok(p) = b.build() {
        window.paint_path(p, color);
    }
}

pub(super) fn paint_line(window: &mut Window, x0: f32, y0: f32, x1: f32, y1: f32, width: f32, color: Rgba) {
    let mut b = PathBuilder::stroke(px(width));
    b.move_to(Point {
        x: px(x0),
        y: px(y0),
    });
    b.line_to(Point {
        x: px(x1),
        y: px(y1),
    });
    if let Ok(p) = b.build() {
        window.paint_path(p, color);
    }
}

pub(super) fn paint_circle(window: &mut Window, cx: f32, cy: f32, r: f32, width: f32, color: Rgba, segs: u32) {
    if r <= 0.5 || segs < 3 {
        return;
    }
    let mut b = PathBuilder::stroke(px(width));
    for i in 0..=segs {
        let a = i as f32 / segs as f32 * std::f32::consts::TAU;
        let x = cx + r * a.cos();
        let y = cy + r * a.sin();
        let p = Point { x: px(x), y: px(y) };
        if i == 0 {
            b.move_to(p);
        } else {
            b.line_to(p);
        }
    }
    if let Ok(path) = b.build() {
        window.paint_path(path, color);
    }
}

pub(super) fn paint_filled_disc(window: &mut Window, cx: f32, cy: f32, r: f32, color: Rgba, segs: u32) {
    if r <= 0.0 || segs < 3 {
        return;
    }
    let mut b = PathBuilder::fill();
    for i in 0..=segs {
        let a = i as f32 / segs as f32 * std::f32::consts::TAU;
        let x = cx + r * a.cos();
        let y = cy + r * a.sin();
        let p = Point { x: px(x), y: px(y) };
        if i == 0 {
            b.move_to(p);
        } else {
            b.line_to(p);
        }
    }
    if let Ok(path) = b.build() {
        window.paint_path(path, color);
    }
}

pub(super) fn paint_closed_polygon(
    window: &mut Window,
    pts: &[(f32, f32)],
    fill: Option<Rgba>,
    stroke: Option<(Rgba, f32)>,
) {
    if pts.len() < 3 {
        return;
    }
    if let Some(fill_color) = fill {
        let mut b = PathBuilder::fill();
        let (x0, y0) = pts[0];
        b.move_to(Point {
            x: px(x0),
            y: px(y0),
        });
        for &(x, y) in &pts[1..] {
            b.line_to(Point { x: px(x), y: px(y) });
        }
        b.line_to(Point {
            x: px(x0),
            y: px(y0),
        });
        if let Ok(p) = b.build() {
            window.paint_path(p, fill_color);
        }
    }
    if let Some((stroke_color, w)) = stroke {
        let mut b = PathBuilder::stroke(px(w));
        let (x0, y0) = pts[0];
        b.move_to(Point {
            x: px(x0),
            y: px(y0),
        });
        for &(x, y) in &pts[1..] {
            b.line_to(Point { x: px(x), y: px(y) });
        }
        b.line_to(Point {
            x: px(x0),
            y: px(y0),
        });
        if let Ok(p) = b.build() {
            window.paint_path(p, stroke_color);
        }
    }
}

