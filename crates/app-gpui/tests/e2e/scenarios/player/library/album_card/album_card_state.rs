// Auto-generated by r2factor. Manual edits will be overwritten on next split.

use super::album_metadata::AlbumMetadata;
use super::types::ArtworkState;
use super::types::ViewMode;

/// Album card state for testing
pub(super) struct AlbumCardState {
    pub(super) metadata: AlbumMetadata,
    pub(super) artwork: ArtworkState,
    pub(super) view_mode: ViewMode,
    pub(super) is_selected: bool,
    pub(super) is_playing: bool,
    pub(super) is_hovered: bool,
    pub(super) show_play_button: bool,
    pub(super) show_track_list: bool,
    pub(super) expanded: bool,
}

impl Default for AlbumCardState {
    fn default() -> Self {
        Self {
            metadata: AlbumMetadata::default(),
            artwork: ArtworkState::Missing,
            view_mode: ViewMode::Grid,
            is_selected: false,
            is_playing: false,
            is_hovered: false,
            show_play_button: false,
            show_track_list: false,
            expanded: false,
        }
    }
}

