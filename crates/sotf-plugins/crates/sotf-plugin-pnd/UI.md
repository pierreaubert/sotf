# PND UI

Present PND as a pitch-motion monitor. Show drift ratio, confidence, matched
partials, and total peaks. Analysis window, multi-channel analysis, and
confidence threshold remain configurable.

Correction strength and phase vocoder are compatibility fields, not active
controls. Render them disabled with help text explaining that correction needs
an explicit clock/pilot reference and a non-fixed-frame processing boundary.
