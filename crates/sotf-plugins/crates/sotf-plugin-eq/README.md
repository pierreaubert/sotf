# sotf-plugin-eq

SOTF EQ plugin with parametric biquad filters.

Parametric equalizer supporting multiple biquad filter types (peak, shelf, highpass, lowpass, etc.) with optional auto-gain normalization.
