//! Re-export of `gpui_design` — the canonical design system now lives in the
//! `gpui-design` crate so that GPUI-toolkit consumers can depend on it without
//! pulling in the full `sotf-host`.

pub use gpui_design::*;
