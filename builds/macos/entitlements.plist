<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Hardened Runtime -->
    <key>com.apple.security.app-sandbox</key>
    <false/>

    <!-- Allow JIT compilation (required by some Rust libraries) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (required by rustfft/realfft) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow dyld environment variables (for debugging) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Audio Input (for recording/calibration) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- File Access -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Network (for spinorama.org API access) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Camera access (for QR code scanning) -->
    <key>com.apple.security.device.camera</key>
    <true/>
</dict>
</plist>
