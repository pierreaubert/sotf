@c -*-texinfo-*-
@c Per-plugin manual: declick
@node declick Plugin
@chapter Plugin: @code{declick}
@cindex declick plugin

Removes clicks, pops, and short transient defects from audio (vinyl restoration, recording artifacts).

@menu
* declick Plugin Parameters::
* declick Plugin Developer Guide::
@end menu

@node declick Plugin Parameters
@section Parameters


@subsection General

@multitable @columnfractions .22 .18 .15 .10 .08 .27
@headitem Parameter @tab Type @tab Range @tab Default @tab Unit @tab Description
@item @code{Enabled} @tab Bool @tab On / Off @tab On @tab - @tab Enable time-domain click repair
@item @code{Sensitivity} @tab Float @tab 1 .. 100 @tab 10 @tab - @tab Click detection sensitivity; lower values detect more clicks
@end multitable

@node declick Plugin Developer Guide
@section Developer Guide

SOTF declick plugin — time-domain click and transient repair.

Wraps the @code{TransientSuppressor} block from @code{plugins-denoiser} in the SOTF host plugin trait.

@subsubheading Parameter wiring

All parameters reach the audio thread through the host's @code{rebuild_cached_parameters} / @code{set_parameter} / @code{get_parameter} triad. Structural parameters (marked in the notes below) trigger a rebuild; the rest update in real time with zero dropouts.

@subsubheading Source

@file{crates/sotf-plugins/crates/sotf-plugin-declick/}

Run the unit tests with @code{cargo test -p sotf-plugin-declick}.

