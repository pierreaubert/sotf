@c -*-texinfo-*-
@c Per-plugin manual: resampler
@node resampler Plugin
@chapter Plugin: @code{resampler}
@cindex resampler plugin

High-quality sample rate conversion using rubato with sinc interpolation for transparent resampling between any sample rates.

@menu
* resampler Plugin Parameters::
* resampler Plugin Developer Guide::
@end menu

@node resampler Plugin Parameters
@section Parameters


@subsection Quality

@multitable @columnfractions .22 .18 .15 .10 .08 .27
@headitem Parameter @tab Type @tab Range @tab Default @tab Unit @tab Description
@item @code{Quality} @tab Choice (Fast, Medium, High) @tab 3 options @tab Medium @tab - @tab Resampling quality: fast (64-tap), medium (128-tap), high (256-tap)
@end multitable


@subsection Ratio

@multitable @columnfractions .22 .18 .15 .10 .08 .27
@headitem Parameter @tab Type @tab Range @tab Default @tab Unit @tab Description
@item @code{Dynamic Ratio} @tab Bool @tab On / Off @tab Off @tab - @tab Enable runtime ratio changes without rebuilding
@item @code{Ratio} @tab Float @tab nominal / 2 .. nominal * 2 @tab Nominal @tab - @tab Current resampling ratio (only adjustable when Dynamic Ratio is enabled)
@end multitable


@strong{Structural parameters} (Quality) require rebuilding the plugin when changed. Other parameters update in real-time with zero dropout.

@node resampler Plugin Developer Guide
@section Developer Guide

SOTF Resampler plugin for sample rate conversion.

High-quality sample rate conversion using rubato with sinc interpolation for transparent resampling between any sample rates.

@subsubheading Parameter wiring

All parameters reach the audio thread through the host's @code{rebuild_cached_parameters} / @code{set_parameter} / @code{get_parameter} triad. Structural parameters (marked in the notes below) trigger a rebuild; the rest update in real time with zero dropouts.

@subsubheading Source

@file{crates/sotf-plugins/crates/sotf-plugin-resampler/}

Run the unit tests with @code{cargo test -p sotf-plugin-resampler}.

