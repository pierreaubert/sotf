@c -*-texinfo-*-
@c Per-plugin manual: gain
@node gain Plugin
@chapter Plugin: @code{gain}
@cindex gain plugin

Simple volume control with smooth gain ramping to prevent clicks.

@menu
* gain Plugin Parameters::
* gain Plugin Developer Guide::
@end menu

@node gain Plugin Parameters
@section Parameters


@subsection General

@multitable @columnfractions .22 .18 .15 .10 .08 .27
@headitem Parameter @tab Type @tab Range @tab Default @tab Unit @tab Description
@item @code{Gain} @tab Float @tab -60 .. 20 @tab 0 @tab dB @tab Volume level adjustment
@item @code{Smoothing} @tab Float @tab 0 .. 100 @tab 10 @tab ms @tab Transition time for gain changes
@end multitable

@node gain Plugin Developer Guide
@section Developer Guide

SOTF Gain plugin for volume control.

Simple SIMD-optimized gain plugin with per-channel support for independent level adjustment.

@subsubheading Parameter wiring

All parameters reach the audio thread through the host's @code{rebuild_cached_parameters} / @code{set_parameter} / @code{get_parameter} triad. Structural parameters (marked in the notes below) trigger a rebuild; the rest update in real time with zero dropouts.

@subsubheading Source

@file{crates/sotf-plugins/crates/sotf-plugin-gain/}

Run the unit tests with @code{cargo test -p sotf-plugin-gain}.

