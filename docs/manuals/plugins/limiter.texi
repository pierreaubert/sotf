@c -*-texinfo-*-
@c Per-plugin manual: limiter
@node limiter Plugin
@chapter Plugin: @code{limiter}
@cindex limiter plugin

Peak limiter to prevent clipping. Ensures output never exceeds the ceiling level.

@menu
* limiter Plugin Parameters::
* limiter Plugin Developer Guide::
@end menu

@node limiter Plugin Parameters
@section Parameters


@subsection Dynamics

@multitable @columnfractions .22 .18 .15 .10 .08 .27
@headitem Parameter @tab Type @tab Range @tab Default @tab Unit @tab Description
@item @code{Threshold} @tab Float @tab -20 .. 0 @tab -0.1 @tab dB @tab Ceiling level (max output)
@item @code{Soft Knee} @tab Bool @tab On / Off @tab Hard @tab - @tab Gradual vs hard limiting onset
@end multitable


@subsection Timing

@multitable @columnfractions .22 .18 .15 .10 .08 .27
@headitem Parameter @tab Type @tab Range @tab Default @tab Unit @tab Description
@item @code{Release} @tab Float @tab 10 .. 1000 @tab 50 @tab ms @tab Time to return to unity gain
@item @code{Lookahead} @tab Float @tab 0 .. 20 @tab 5 @tab ms @tab Pre-delay for peak catching
@item @code{Dual Release} @tab Bool @tab On / Off @tab Off @tab - @tab Fast+slow release envelopes
@end multitable


@subsection Detection

@multitable @columnfractions .22 .18 .15 .10 .08 .27
@headitem Parameter @tab Type @tab Range @tab Default @tab Unit @tab Description
@item @code{True Peak} @tab Bool @tab On / Off @tab Off @tab - @tab Detect inter-sample peaks
@item @code{ISP Limit} @tab Bool @tab On / Off @tab Off @tab - @tab Guarantee output has no inter-sample peaks above ceiling
@item @code{Link} @tab Float @tab 0 .. 1 @tab 1 @tab % @tab Channel linking: 0%=independent, 100%=linked (all channels see max peak)
@item @code{Feed Forward} @tab Bool @tab On / Off @tab Off @tab - @tab Scan lookahead buffer for anticipatory gain reduction
@end multitable


@subsection Output

@multitable @columnfractions .22 .18 .15 .10 .08 .27
@headitem Parameter @tab Type @tab Range @tab Default @tab Unit @tab Description
@item @code{Mix} @tab Float @tab 0 .. 1 @tab 1 @tab % @tab Dry/wet blend
@end multitable

@node limiter Plugin Developer Guide
@section Developer Guide

SOTF Limiter plugin for peak limiting.

Prevents audio from exceeding a set ceiling by applying fast gain reduction to peaks, protecting downstream equipment and preventing clipping.

@subsubheading Parameter wiring

All parameters reach the audio thread through the host's @code{rebuild_cached_parameters} / @code{set_parameter} / @code{get_parameter} triad. Structural parameters (marked in the notes below) trigger a rebuild; the rest update in real time with zero dropouts.

@subsubheading Source

@file{crates/sotf-plugins/crates/sotf-plugin-limiter/}

Run the unit tests with @code{cargo test -p sotf-plugin-limiter}.

