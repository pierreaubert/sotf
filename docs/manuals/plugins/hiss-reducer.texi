@c -*-texinfo-*-
@c Per-plugin manual: hiss-reducer
@node hiss-reducer Plugin
@chapter Plugin: @code{hiss-reducer}
@cindex hiss-reducer plugin

Reduces high-frequency hiss and tape noise while preserving program content.

@menu
* hiss-reducer Plugin Parameters::
* hiss-reducer Plugin Developer Guide::
@end menu

@node hiss-reducer Plugin Parameters
@section Parameters


@subsection General

@multitable @columnfractions .22 .18 .15 .10 .08 .27
@headitem Parameter @tab Type @tab Range @tab Default @tab Unit @tab Description
@item @code{Enabled} @tab Bool @tab On / Off @tab On @tab - @tab Enable high-frequency hiss reduction
@item @code{Threshold} @tab Float @tab -60 .. -10 @tab -30 @tab dB @tab SNR threshold for stationary hiss detection
@item @code{Frequency} @tab Float @tab 1000 .. 16000 @tab 4000 @tab Hz @tab Frequency above which hiss reduction applies
@item @code{Strength} @tab Float @tab 0 .. 1 @tab 0.5 @tab - @tab Hiss attenuation strength
@end multitable

@node hiss-reducer Plugin Developer Guide
@section Developer Guide

SOTF hiss reducer plugin — stationary high-frequency noise reduction.

Wraps the @code{HissReducer} block from @code{plugins-denoiser} in the SOTF host plugin trait.

@subsubheading Parameter wiring

All parameters reach the audio thread through the host's @code{rebuild_cached_parameters} / @code{set_parameter} / @code{get_parameter} triad. Structural parameters (marked in the notes below) trigger a rebuild; the rest update in real time with zero dropouts.

@subsubheading Source

@file{crates/sotf-plugins/crates/sotf-plugin-hiss-reducer/}

Run the unit tests with @code{cargo test -p sotf-plugin-hiss-reducer}.

