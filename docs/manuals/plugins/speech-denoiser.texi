@c -*-texinfo-*-
@c Per-plugin manual: speech-denoiser
@node speech-denoiser Plugin
@chapter Plugin: @code{speech-denoiser}
@cindex speech-denoiser plugin

Neural speech-focused denoiser (RNNoise-derived) optimized for voice and dialogue cleanup.

@menu
* speech-denoiser Plugin Parameters::
* speech-denoiser Plugin Developer Guide::
@end menu

@node speech-denoiser Plugin Parameters
@section Parameters

This plugin has no user-tunable parameters.

@node speech-denoiser Plugin Developer Guide
@section Developer Guide

SOTF speech denoiser plugin — RNNoise voice denoising.

Wraps the @code{RnnoiseBackend} block (which itself wraps @code{nnnoiseless}) from @code{plugins-denoiser} in the SOTF host plugin trait.

@subsubheading Parameter wiring

All parameters reach the audio thread through the host's @code{rebuild_cached_parameters} / @code{set_parameter} / @code{get_parameter} triad. Structural parameters (marked in the notes below) trigger a rebuild; the rest update in real time with zero dropouts.

@subsubheading Source

@file{crates/sotf-plugins/crates/sotf-plugin-speech-denoiser/}

Run the unit tests with @code{cargo test -p sotf-plugin-speech-denoiser}.

