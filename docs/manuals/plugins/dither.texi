@c -*-texinfo-*-
@c Per-plugin manual: dither
@node dither Plugin
@chapter Plugin: @code{dither}
@cindex dither plugin

Adds dither noise for bit-depth reduction, minimizing quantization distortion.

@menu
* dither Plugin Parameters::
* dither Plugin Developer Guide::
@end menu

@node dither Plugin Parameters
@section Parameters

This plugin has no user-tunable parameters.

@node dither Plugin Developer Guide
@section Developer Guide

TPDF dither with F-weighted noise shaping for bit-depth reduction.

@subsubheading What It Does

When reducing audio bit depth (e.g., 24-bit to 16-bit for CD), quantization distortion is introduced. Dithering adds a small amount of shaped noise to eliminate this distortion, replacing it with a low-level, perceptually benign noise floor. F-weighted noise shaping pushes the dither noise into frequency ranges where human hearing is least sensitive.

@subsubheading Features

@itemize @bullet
@item @strong{TPDF dither}: Triangular Probability Density Function for optimal quantization noise decorrelation
@item @strong{F-weighted noise shaping}: Pushes noise to less audible frequencies
@item @strong{Configurable target bit depth}: 8, 16, 20, 24 bit targets
@end itemize


@subsubheading Architecture

@example
src/
├── lib.rs     # DitherPlugin implementation
└── params.rs  # Parameter definitions
@end example


@subsubheading Testing

@example
cargo test -p sotf-plugin-dither
@end example


@subsubheading Parameter wiring

All parameters reach the audio thread through the host's @code{rebuild_cached_parameters} / @code{set_parameter} / @code{get_parameter} triad. Structural parameters (marked in the notes below) trigger a rebuild; the rest update in real time with zero dropouts.

@subsubheading Source

@file{crates/sotf-plugins/crates/sotf-plugin-dither/}

Run the unit tests with @code{cargo test -p sotf-plugin-dither}.

