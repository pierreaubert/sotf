@c -*-texinfo-*-
@c Per-plugin manual: spectrum-analyzer
@node spectrum-analyzer Plugin
@chapter Plugin: @code{spectrum-analyzer}
@cindex spectrum-analyzer plugin

FFT-based spectrum analysis with configurable bin count, frequency range, smoothing, and tilt correction.

@menu
* spectrum-analyzer Plugin Parameters::
* spectrum-analyzer Plugin Developer Guide::
@end menu

@node spectrum-analyzer Plugin Parameters
@section Parameters


@subsection General

@multitable @columnfractions .22 .18 .15 .10 .08 .27
@headitem Parameter @tab Type @tab Range @tab Default @tab Unit @tab Description
@item @code{Num Bins} @tab Int @tab 8 .. 120 @tab 30 @tab - @tab Number of frequency bands
@item @code{Min Freq} @tab Float @tab 10 .. 100 @tab 20 @tab Hz @tab Lowest displayed frequency
@item @code{Max Freq} @tab Float @tab 5000 .. 22050 @tab 20000 @tab Hz @tab Highest displayed frequency
@item @code{Smoothing} @tab Float @tab 0 .. 1 @tab 0.7 @tab - @tab Temporal averaging factor
@item @code{Tilt Correction} @tab Choice (None, 3dB/oct, 6dB/oct, Pink) @tab 4 options @tab None @tab - @tab Slope compensation for display
@item @code{Tilt Reference} @tab Choice (Standard, 1kHz, 2kHz, Min Freq) @tab 4 options @tab Standard @tab - @tab Reference frequency for tilt
@end multitable


@strong{Structural parameters} (Num Bins, Tilt Correction, Tilt Reference) require rebuilding the plugin when changed. Other parameters update in real-time with zero dropout.

@node spectrum-analyzer Plugin Developer Guide
@section Developer Guide

Tap-style analyser. Audio is windowed (Hann by default) and fed through an FFT; magnitudes are smoothed across frames and passed to the UI through a lock-free SPSC channel. No processing is applied to the audio signal.

@subsubheading Parameter wiring

All parameters reach the audio thread through the host's @code{rebuild_cached_parameters} / @code{set_parameter} / @code{get_parameter} triad. Structural parameters (marked in the notes below) trigger a rebuild; the rest update in real time with zero dropouts.

@subsubheading Source

The @code{spectrum-analyzer} plugin does not have a dedicated crate at the time of writing; its implementation lives under @file{crates/sotf-plugins/crates/sotf-host/} or one of the multi-component crates. Search the workspace for the plugin type key @code{"spectrum-analyzer"} to locate it.

Run the unit tests with @code{cargo test -p sotf-plugin-spectrum-analyzer}.

