{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -er '.tool_input.file_path | endswith(\".rs\")' >/dev/null 2>&1 && cargo check --workspace --message-format=short 2>&1 | head -20 || true"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "code-simplifier@claude-plugins-official": true
  }
}
